{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "In-app updater (check/download/install) + relaunch. Desktop-only: Android updates ship as a signed APK, not via the updater plugin.",
  "platforms": ["windows", "macOS", "linux"],
  "windows": ["main", "popout-*"],
  "permissions": ["updater:default", "process:allow-restart", "process:default"]
}
